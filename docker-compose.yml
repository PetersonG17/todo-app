version: '3.8'

services:
  todoapp:
    image: todoapp
    build:
      context: ./
      target: dev
    volumes:
      - .:/src
    command: "npm run start:dev"
    environment:
      NODE_ENV: development
      DEBUG: nodejs-docker-express:*
    ports:
      - 3000:3000
